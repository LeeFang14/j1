import { Q8 } from "./Q8.js";

describe("測試 Q8 預期應輸出", () => {
  test("輸入:3，應輸出:最少要除 1 次", () => {
    expect(Q8("3")).toBe("最少要除 1 次");
  });
  test("輸入:10，應輸出:最少要除 7 次", () => {
    expect(Q8("10")).toBe("最少要除 7 次");
  });
});
